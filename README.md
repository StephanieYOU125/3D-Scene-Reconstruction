# 3D-Scene-Reconstruction
This project implements a 3D scene reconstruction pipeline using RGB-D data. The system reconstructs point clouds from RGB and depth images and visualizes the reconstructed environment.
